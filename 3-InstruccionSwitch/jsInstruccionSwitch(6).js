function mostrar()
{
//tomo la edad  
var laHora = document.getElementById('hora').value;

/*hace acciones basadas en condiciones.
se evaluna una vez, si hay coincidencia se
ejecuta el bloque correspondiente*/

switch (laHora)
{
    case "Es de mañana":
        if (laHora >=7 && laHora <= 11 )
        {
            alert ("Es de mañana")
        } 
        else
}
}//FIN DE LA FUNCIÓN